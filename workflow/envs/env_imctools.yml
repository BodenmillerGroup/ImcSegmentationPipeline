name: imctools
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - numpy
  - python=3.8
  - pandas
  - pip
  - pip:
      - git+https://github.com/BodenmillerGroup/imctools.git@next
